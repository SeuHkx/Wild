{
  "name":{
    "gulp-autoprefixer": "prefix",
    "gulp-minify-css": "mini",
    "gulp-rename": "rename",
    "gulp-sass": "sass",
    "gulp-clean": "clean",
    "gulp-if": "if"
  },
  "dir" : {
    "sass" : "sass/**/*.scss",
    "dist" : "dist/",
    "css"  : "dist/**/*.css",
    "widget": "widget/"
  }
}
